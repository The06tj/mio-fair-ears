{
  "name": "mio-fair-ears",
  "version": "0.1.0",
  "private": true,
  "description": "A local, loudness-matched blind listening room for two audio files.",
  "type": "module",
  "license": "MIT",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build.mjs"
  }
}
